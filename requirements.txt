# ── Core Web Framework ────────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.0
flask-jwt-extended==4.6.0
gunicorn==22.0.0

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# ── Scraping / Automation (used by agent, not needed just to boot the API) ────
# These are intentionally kept here so "pip install -r requirements.txt" still
# works in local dev. On Render the build installs ALL of these — that is OK
# because Render caches the venv between deploys. Playwright browser binaries
# are NOT installed here; the agent runs headless via a separate task.
playwright==1.42.0
beautifulsoup4==4.12.3

# ── ML / NLP ──────────────────────────────────────────────────────────────────
scikit-learn==1.4.2
spacy==3.7.4

# ── Document Parsing ─────────────────────────────────────────────────────────
pdfplumber==0.11.0
docx2txt==0.8

# ── Google Integrations ───────────────────────────────────────────────────────
gspread==6.0.2
google-auth==2.29.0
