{
  "version": 2,
  "name": "autoapply-ai",
  "buildCommand": "cd finder-ui && npm install && npm run build",
  "outputDirectory": "finder-ui/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
